# Install authoritative stack: pip install -e . (see mechanism/setup.py).
# This file lists the same floor versions as setup.py for CI / quick venvs.
bittensor>=10.0.0
torch>=2.0.0
numpy>=2.0.1,<3.0.0
requests>=2.31.0
